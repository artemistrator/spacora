# Space Social - Социальная сеть для жилых пространств

Space Social - это социальная сеть, которая позволяет пользователям создавать профили своих жилых пространств, публиковать фотографии интерьеров и использовать AI для замены предметов на фото из избранного.

## Основные возможности

### 🏠 Профили пространств
- Создание профилей для квартир, домов, студий и других жилых пространств
- Загрузка аватаров и обложек
- Публичные и приватные профили

### 📸 Публикация постов
- Публикация фотографий интерьеров
- Добавление описаний и тегов
- Категоризация по типам комнат (кухня, спальня, гостиная и т.д.)

### 🤖 AI замена объектов
- Замена предметов на фотографиях с помощью AI
- Использование избранных объектов для замены
- Интеграция с N8N для обработки изображений

### 🎮 Геймификация
- Система очков и уровней
- Достижения и награды
- Статистика активности

## Технологический стек

```json
{
  "frontend": ["Next.js 15", "React 19", "TypeScript", "Tailwind CSS", "Shadcn/UI"],
  "backend": ["Next.js API Routes", "Supabase PostgreSQL", "Supabase Storage"],
  "auth": ["Clerk + Supabase Integration"],
  "automation": ["N8N workflows (обрабатывает AI задачи)"],
  "deployment": ["Vercel + Supabase Cloud"]
}
```

## Структура проекта

```
space-social-app/
├── src/
│   ├── app/                    # Next.js App Router
│   │   ├── page.tsx           # Лента постов пространств
│   │   ├── space/[id]/        # Профиль пространства
│   │   ├── post/[id]/         # Просмотр поста
│   │   ├── favorites/         # Избранное пользователя
│   │   ├── ai-studio/         # AI замена объектов (UI часть)
│   │   └── gamification/      # Достижения, уровни
│   │   └── api/              # API endpoints (упрощенные, основная логика в N8N)
│   │       ├── spaces/        # CRUD пространств
│   │       ├── posts/         # CRUD постов
│   │       ├── upload/        # Загрузка в Supabase Storage
│   │       └── webhooks/      # Webhooks для N8N
│   ├── components/
│   │   ├── ui/               # Shadcn компоненты
│   │   ├── feed/             # Лента постов
│   │   ├── space/            # Профили пространств
│   │   ├── post/             # Компоненты постов
│   │   ├── upload/           # Загрузка изображений
│   │   └── gamification/     # Геймификация
│   ├── lib/
│   │   ├── supabase.ts       # Supabase client и типы
│   │   ├── auth.ts           # Clerk + Supabase auth
│   │   ├── n8n.ts            # N8N webhooks calls
│   │   └── utils.ts          # Утилиты
│   ├── hooks/                # Custom React hooks
│   │   ├── useSpaces.ts      # Хуки для работы с пространствами
│   │   ├── usePosts.ts       # Хуки для работы с постами
│   │   └── useAuth.ts        # Хуки для аутентификации
│   └── types/
│       └── database.ts       # Supabase типы
├── supabase/
│   ├── migrations/           # SQL миграции
│   └── config.toml          # Supabase config
└── package.json
```

## Установка и запуск

1. Клонируйте репозиторий:
   ```bash
   git clone <repository-url>
   cd space-social
   ```

2. Установите зависимости:
   ```bash
   npm install
   ```

3. Создайте файл `.env.local` с переменными окружения:
   ```env
   NEXT_PUBLIC_SUPABASE_URL=your-supabase-url
   NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key
   NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=your-clerk-publishable-key
   CLERK_SECRET_KEY=your-clerk-secret-key
   ```

4. Запустите сервер разработки:
   ```bash
   npm run dev
   ```

5. Откройте [http://localhost:3000](http://localhost:3000) в браузере.

## Развертывание

Проект развертывается на Vercel. Для развертывания необходимо:

1. Подключить репозиторий к Vercel
2. Установить переменные окружения в настройках проекта Vercel
3. Настроить Supabase проект и добавить его URL и ключи

## Лицензия

MIT License

## Контакты

Для вопросов и предложений обращайтесь к разработчику.